import { HStack, Icon } from "@chakra-ui/react";

import { IoFilter } from "react-icons/io";

const Filter = () => {
  return (
    <HStack>
      <Icon as={IoFilter} />
    </HStack>
  );
};

export default Filter;
